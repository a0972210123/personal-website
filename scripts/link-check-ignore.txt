# 已確認失效、還沒修好的連結（不讓 CI 一直紅，但仍會列在報告裡）
#
# 這是「待修清單」不是「不用修」。修好一條就從這裡刪一條。
# 語法：一行一個網址，# 開頭是註解。
#
# 注意：不要把「健檢回非 2xx」直接當成失效。2026-07-26 逐條追查後，原本列在這裡的
# 17 條有 14 條其實是好的，只是對方擋機器人或需要登入——那些已經移到
# link-check-verified.txt。判斷前先想一件事：這個網域的其他網址健檢過得了嗎？
# 如果連對方的首頁都「404」，那 404 的意思就不是頁面不存在。

# imagine.art 的 ASMR 教學文（用於 /projects/marketing/faceless-pipeline/ 中英版）。
# 2026-09-07：站方回 HTTP 410 Gone——明確宣告永久移除，不是擋機器人。
# 依這個檔開頭的判準逐項驗過（家用瀏覽器 UA、非機房 IP）：
#   首頁 200、/blogs 200、而 /blogs/<隨便打一個不存在的 slug> 回的是 200 軟 404。
# 也就是說這個站連「頁面不存在」都不給 404，卻單獨對這一篇回 410——
# 那是站方主動刪文的訊號，不是把爬蟲擋掉。
# 待修：faceless-pipeline 的 ASMR 段落要換一個替代來源（內容決定，待 owner 挑）。
https://www.imagine.art/blogs/how-to-create-asmr-videos
#
# 已處理：
# - blogs.sjsu.edu BOPPPS 第 1 篇：站方移除，改連確定還在的第 2 篇本文。
#   （這個網域跟 Google 一樣對機房 IP 一律回 404，所以第 2 篇也驗不到，
#     列在 link-check-verified.txt。）
# - gf.com 兩篇新聞稿：GF 把舊新聞稿整批下架了。舊網址會 302 到新的
#   /news-and-events/news/<slug>/，但那個位置一樣 404，等於文章本身不在了；
#   investors.gf.com 上的版本則從 CI 逾時、驗不到。最後改引通訊社的原始稿
#   （BusinessWire 2021、PRNewswire 2022），這種帶固定編號的稿件會長期保存。
# - csrc.nist.gov 兩條：這個網域時好時壞。連續兩輪 CI 之間，Falcon 簡報頁與 PQC FAQ
#   的結果直接對調，所以兩條都不是失效，改列在 link-check-verified.txt。
